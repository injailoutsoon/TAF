server:
  servlet:
    context-path:
  port: ${PORT_AUTH:8080}

spring:
  application:
    name: auth

  cloud:
    compatibility-verifier:
      enabled: false

  data:
    mongodb:
      uri: ${DB_URI}
      authentication-database: ${DB_AUTH}
      database: ${DB_NAME}
      auto-index-creation: true

  jpa:
    hibernate:
      ddl-auto: none // from update to none because we use mongodb no spring jpa -> fixing conflicts and disables disables Hibernate schema generation
    show-sql: false

feign:
  okhttp:
    enabled: true

taf:
  app:
    jwtSecret: ${API_TEST_TOKEN}
    jwtExpirationMs: ${API_TEST_EXP_TIME}

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  #  prefer-ip-address: true

  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${EUREKA_USERNAME}:${EUREKA_PASSWORD}@${EUREKA_HOST}:${EUREKA_PORT}/eureka

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path:  /swagger-ui.html

logging:
  level:
    root: ${LOG_ROOT:DEBUG}
    org.springframework.web: ${LOG_WEB:WARN}
    org.hibernate: ${LOG_HIBERNATE:WARN}
    org.springframework: ${LOG_FRAMEWORK:WARN}

#endpoints:
#  cors:
#    allowed-origins: "*"
#    allowed-methods: "*"
#    allowed-headers: "*"
#    allow-credentials: true